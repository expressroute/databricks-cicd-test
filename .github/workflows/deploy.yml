name: Deploy Bundle

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to deploy from"
        required: true
        default: "develop"
        type: string
      target:
        description: "Target environment (dev or prd)"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - prd

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the selected branch
      - name: Checkout selected branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      # Install the Databricks CLI (v1)
      - name: Install Databricks CLI v1
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/cli/main/install.sh | bash

      # Install Python SDK (optional)
      - name: Install Databricks Python SDK
        run: pip install databricks-sdk

      # Set Databricks auth env vars
      - name: Set Databricks auth
        run: |
          echo "DATABRICKS_HOST=${{ secrets.DATABRICKS_HOST }}" >> $GITHUB_ENV
          echo "DATABRICKS_TOKEN=${{ secrets.DATABRICKS_TOKEN }}" >> $GITHUB_ENV

      # Deploy using ABSOLUTE PATH (bulletproof)
      - name: Deploy bundle
        run: $HOME/.databricks/bin/databricks bundle deploy --target ${{ github.event.inputs.target }}